{{#each users as |user| }}
<div class="row">
  <div class="col">
    <h2>{{user.name}}</h2>
  </div>
  <div class="col">
    <h3><a href="mailto:{{user.email}}">{{user.email}}</a></h3>
  </div>
</div>
{{/each}}